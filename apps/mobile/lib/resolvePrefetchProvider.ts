/**
 * @deprecated Use resolvePlaybackProvider.ts (canonical FIX 1 resolver).
 * Kept so existing imports keep compiling during the Phase 3A migration.
 */
export {
  resolvePlaybackProviderId as resolvePrefetchProviderId,
} from "./resolvePlaybackProvider";
