/**
 * Shared detail-query constants — one staleTime for hooks and every prefetch.
 * TMDB metadata is essentially static (Phase 2 FIX 4).
 */
export const DAY = 86_400_000;
export const DETAIL_STALE_TIME = 7 * DAY;
