# The root .gitignore's `scripts/` rule is meant for the monorepo-root scripts/
# folder only, but it also swallows this package's build-critical scripts/
# directory (inject-preload-css.js is required by the `build` script). Re-include
# it here so the release build works for anyone who checks out the repo.
!scripts/
